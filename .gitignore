.venv/
.env
.streamlit/secrets.toml
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# raw source PDFs (copyrighted third-party guideline documents) — not committed
data/raw/

# data/processed/ (built Chroma index) IS committed so the Streamlit Cloud
# demo has a working index without re-ingesting PDFs at deploy time.
