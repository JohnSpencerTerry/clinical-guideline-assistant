"""Builds/loads the three per-source vector indices (ADA / NICE / IDF).

Indexed separately per source since the disagreement-detection flow needs to
retrieve from each source independently before comparing.
"""
